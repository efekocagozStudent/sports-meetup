# Copy this file to .env for local overrides (never commit .env)
# On Railway, set these in the Variables tab of your service.

DB_HOST=mysql
DB_NAME=sports_meetup
DB_USER=root
DB_PASS=secret123

# Leave empty when hosting at the root path.
# Set to e.g. /myapp if hosted in a subdirectory.
BASE_URL=
